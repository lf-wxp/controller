# controller-host-demo 使用 host target（不是 xtensa-esp32-none-elf）
# 覆盖仓库根 .cargo/config.toml 的 xtensa 默认设置
#
# 让 `cd crates/examples/controller-host-demo && cargo run` 直接跑到本机

[build]
rustflags = []

# 关闭 build-std（顶层的 alloc/core 只针对 xtensa target）
[unstable]
build-std = []
